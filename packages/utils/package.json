{
  "name": "utils",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "compile:test": "tsc -p tsconfig.test.json",
    "test": "pnpm run compile:test && node --test \"dist/test/**/*.test.js\""
  }
}
